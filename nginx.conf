server {
  listen 80;
  location /api/ { proxy_pass http://backend:8000/api/; }
  location /ai-agent/ { proxy_pass http://ai-agent:8501/; }
  location / { root /usr/share/nginx/html; try_files $uri /index.html; }
}
